@model PrHeredades.Models.tbCategoria

@{
    ViewBag.Title = "Crear";
}

<form action="@Url.Action("Crear")" method="post" id="formCategoria">
    @Html.AntiForgeryToken()
    <h2>Crear categoría</h2>
    <hr />
    <h4>@Html.ValidationSummary(true, "", new { @class = "text-danger" })</h4>
    <div class="form-group">
        <label for="categoria" class="col-form-label">Categoría</label>
        @Html.TextBoxFor(t => t.categoria, new { @class = "form-control col-md-4", autocomplete = "off", maxlength = "50" })
    </div>
    <div class="form-group col-md-4 text-center">
        <button type="submit" class="btn btn-primary" style="width: 150px">
            <span class="fa fa-check"></span> Crear
        </button>
        <a href="@Url.Action("Index")" class="btn btn-danger" style="width: 150px">
            <span class="fa fa-times"></span> Cancelar
        </a>
    </div>
</form>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/jqueryValidator/categoria.js"></script>
}
