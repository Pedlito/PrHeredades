@model PrHeredades.Models.tbPresentacion

@{
    ViewBag.Title = "Editar";
}

<form action="@Url.Action("Editar")" method="post" id="formPresentacion">
    @Html.AntiForgeryToken()
    <h2>Editar Presentación</h2>
    <hr />
    <h4>@Html.ValidationSummary(true, "", new { @class = "text-danger" })</h4>
    @Html.HiddenFor(m => m.codPresentacion)
    @Html.HiddenFor(m => m.estado)
    <div class="form-group">
        <label for="presentacion" class="col-form-label">Presentación</label>
        @Html.TextBoxFor(m => m.presentacion, new { @class = "form-control col-md-4", autocomplete = "off", maxlength = "50" })
    </div>
    <div class="form-group col-md-4 text-center" style="padding-left:0; padding-right:0">
        <button type="submit" class="btn btn-primary" style="width: 150px">
            <span class="fa fa-check"></span> Editar
        </button>
        <a href="@Url.Action("Index")" class="btn btn-danger" style="width: 150px">
            <span class="fa fa-times"></span> Cancelar
        </a>
    </div>
</form>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/jqueryValidator/presentacion.js"></script>
}
